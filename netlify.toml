[build]
  command = "npm run build"
  publish = "dist"
  functions = "functions"

[functions]
  node_bundler = "esbuild"

# SPA routing: send all paths to index.html so React Router works on Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

